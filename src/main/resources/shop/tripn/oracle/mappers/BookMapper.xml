<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shop.tripn.oracle.mappers.BookMapper">
	<select id="findAll" resultType="shop.tripn.oracle.book.domain.BookDto">
		select 
			book_id bookId,
			book_title bookTitle,
			price,
			pub_id pubId
		 from books
	</select>
	<select id="findById" resultType="shop.tripn.oracle.book.domain.BookDto" 
	parameterType="int">
		select 
			book_id bookId,
			book_title bookTitle,
			price,
			pub_id pubId
		 from books
         where book_id like ${bookId}
	</select>
</mapper>